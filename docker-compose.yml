version: "3.7"

services: 
  database_ignite:
    image: postgres
    container_name: database_ignite
    restart: always
    ports:
      - 5432:5432 
    environment:
      - POSTGRES_USER=docker
      - POSTGRES_PASSWORD=ignite
      - POSTGRES_DB=rentx
    volumes:
      - pgdata:/data/postgres

  redis:
    image: redis:alpine
    ports: 
      - 6379:6379

  # app:                    # Nome do app 
  #   build: .              # Roda o docker build
  #   container_name: rentx # Nome do container
  #   ports:
  #     - 3333:3333         # Mapeia a porta 3333
  #   volumes:
  #     - .:/usr/app        # Diretório
  #   links:
  #     - database_ignite
  #   depends_on:
  #     - database_ignite

volumes:
  pgdata:
    driver: local